#!/bin/sh
# Refresh AGENTS.md from BACKLOG + git log before each commit.
set -e
ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT"
node scripts/agent-sync.mjs sync
if ! git diff --quiet -- AGENTS.md 2>/dev/null; then
  git add AGENTS.md
fi
